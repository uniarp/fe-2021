import { ReservaEquipamento } from './reserva-equipamento';

describe('ReservaEquipamento', () => {
  it('should create an instance', () => {
    expect(new ReservaEquipamento()).toBeTruthy();
  });
});
