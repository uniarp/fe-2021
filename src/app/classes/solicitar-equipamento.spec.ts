import { SolicitarEquipamento } from './solicitar-equipamento';

describe('SolicitarEquipamento', () => {
  it('should create an instance', () => {
    expect(new SolicitarEquipamento()).toBeTruthy();
  });
});
